#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../css-minify/bin/css-minify.js" "$@"
  ret=$?
else 
  node  "$basedir/../css-minify/bin/css-minify.js" "$@"
  ret=$?
fi
exit $ret
